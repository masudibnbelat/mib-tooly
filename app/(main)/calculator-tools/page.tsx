import CalculatorTools from "@/src/components/CalculatorTools/CalculatorTools";

const page = () => {
  return (
    <div className="h-screen">
      <CalculatorTools />
    </div>
  );
};

export default page;
